# คู่มือการเปิดไฟล์ .tif ใน QGIS

## 📋 ขั้นตอนการเปิดไฟล์

### วิธีที่ 1: ลากไฟล์เข้า QGIS (ง่ายที่สุด)

1. **เปิดโปรแกรม QGIS**
   - เปิด QGIS Desktop

2. **ลากไฟล์เข้าไป**
   - เปิด File Explorer ไปที่โฟลเดอร์ `data`
   - ลากไฟล์ `.tif` ที่ต้องการ (เช่น `Monthly_LST_Filled_2018-2025.tif`)
   - วางลงในหน้าต่าง QGIS

3. **เลือก Band ที่ต้องการ**
   - ไฟล์ของคุณมี 94 bands (94 เดือน)
   - QGIS จะถามว่าต้องการเปิด band ไหน
   - เลือก band ที่ต้องการดู หรือเลือก "Add all bands"

---

### วิธีที่ 2: ใช้เมนู Layer

1. **เปิด QGIS**

2. **เพิ่ม Raster Layer**
   - คลิก `Layer` → `Add Layer` → `Add Raster Layer...`
   - หรือกด `Ctrl + Shift + R`

3. **เลือกไฟล์**
   - คลิกปุ่ม `...` ข้าง Source
   - เลือกไฟล์ `.tif` จากโฟลเดอร์ `data`
   - คลิก `Add`

4. **ปิดหน้าต่าง**
   - คลิก `Close`

---

### วิธีที่ 3: ใช้ Browser Panel

1. **เปิด Browser Panel**
   - ไปที่ `View` → `Panels` → `Browser`
   - หรือกด `Ctrl + 2`

2. **นำทางไปยังโฟลเดอร์**
   - ใน Browser Panel ขยาย `Home` หรือ `Computer`
   - นำทางไปยังโฟลเดอร์ `data` ของคุณ

3. **ดับเบิลคลิกไฟล์**
   - ดับเบิลคลิกที่ไฟล์ `.tif` ที่ต้องการ

---

## 🎨 การปรับแต่งการแสดงผล

### 1. เปลี่ยนสีของแผนที่

1. **เปิด Layer Properties**
   - คลิกขวาที่ layer → `Properties`
   - หรือดับเบิลคลิกที่ layer

2. **ไปที่แท็บ Symbology**
   - เลือก `Render type`: `Singleband pseudocolor`

3. **เลือก Color ramp**
   - คลิกที่ dropdown สี
   - เลือกสีที่เหมาะสม เช่น:
     - **Reds**: สำหรับอุณหภูมิ (LST)
     - **Greens**: สำหรับพืชพรรณ (NDVI)
     - **Spectral**: สำหรับข้อมูลทั่วไป

4. **ปรับ Min/Max values**
   - คลิก `Min/Max Value Settings`
   - เลือก `Cumulative count cut` (2% - 98%)
   - คลิก `Apply`

### 2. ปรับความโปร่งใส

1. **ไปที่แท็บ Transparency**
   - ตั้งค่า `Global Opacity` (0-100%)

2. **ตั้งค่า No Data**
   - ใส่ค่า No Data ถ้ามี (เช่น -9999)

### 3. เพิ่ม Labels

1. **ไปที่แท็บ Labels**
   - เลือก `Single Labels`
   - เลือก field ที่ต้องการแสดง

---

## 📊 การดูข้อมูลแต่ละ Band (เดือน)

ไฟล์ของคุณมี 94 bands = 94 เดือน (2018-01 ถึง 2025-10)

### วิธีที่ 1: เปิดทีละ Band

1. **เปิดไฟล์**
   - เมื่อเปิดไฟล์ เลือก band เดียว (เช่น Band 1)

2. **เปลี่ยน Band**
   - คลิกขวาที่ layer → `Properties`
   - ไปที่แท็บ `Symbology`
   - เปลี่ยน `Band` จาก dropdown

### วิธีที่ 2: สร้าง Animation (Time Series)

1. **เปิดทุก Bands**
   - เมื่อเปิดไฟล์ เลือก "Add all bands"

2. **ใช้ Temporal Controller**
   - ไปที่ `View` → `Panels` → `Temporal Controller`
   - ตั้งค่าช่วงเวลา
   - กด Play เพื่อดู animation

### วิธีที่ 3: ใช้ Plugin "Value Tool"

1. **ติดตั้ง Plugin**
   - `Plugins` → `Manage and Install Plugins...`
   - ค้นหา "Value Tool"
   - คลิก `Install Plugin`

2. **เปิด Value Tool**
   - `Plugins` → `Value Tool` → `Value Tool`
   - คลิกที่แผนที่เพื่อดูค่าของทุก bands

---

## 🗺️ การเพิ่ม Basemap

### เพิ่มแผนที่พื้นฐาน (OpenStreetMap)

1. **เปิด Browser Panel**
   - `View` → `Panels` → `Browser`

2. **เพิ่ม XYZ Tiles**
   - ขยาย `XYZ Tiles`
   - ดับเบิลคลิก `OpenStreetMap`

### เพิ่ม Google Satellite (ต้องติดตั้ง Plugin)

1. **ติดตั้ง QuickMapServices**
   - `Plugins` → `Manage and Install Plugins...`
   - ค้นหา "QuickMapServices"
   - คลิก `Install Plugin`

2. **เปิดใช้งาน**
   - `Web` → `QuickMapServices` → `Google` → `Google Satellite`

---

## 📈 การวิเคราะห์ข้อมูล

### 1. ดูค่าสถิติ

1. **เปิด Raster Layer Statistics**
   - คลิกขวาที่ layer → `Properties`
   - ไปที่แท็บ `Information`
   - ดูค่า Min, Max, Mean, Std Dev

### 2. สร้าง Histogram

1. **เปิด Raster Histogram**
   - คลิกขวาที่ layer → `Properties`
   - ไปที่แท็บ `Histogram`
   - คลิก `Compute Histogram`

### 3. ดูค่าที่จุดเฉพาะ

1. **ใช้ Identify Tool**
   - คลิกปุ่ม `Identify Features` (หรือกด `Ctrl + Shift + I`)
   - คลิกที่จุดบนแผนที่
   - ดูค่าที่แสดง

---

## 💾 การ Export ภาพ

### Export เป็นรูปภาพ (PNG, JPG)

1. **ปรับมุมมองให้เหมาะสม**
   - ซูมและเลื่อนแผนที่ให้ได้มุมที่ต้องการ

2. **Export**
   - `Project` → `Import/Export` → `Export Map to Image...`
   - ตั้งค่า:
     - **Resolution**: 300 DPI (สำหรับพิมพ์) หรือ 150 DPI (สำหรับหน้าจอ)
     - **Output width/height**: ตามต้องการ
   - คลิก `Save`

### สร้าง Print Layout (สำหรับรายงาน)

1. **สร้าง Layout ใหม่**
   - `Project` → `New Print Layout...`
   - ตั้งชื่อ layout

2. **เพิ่มแผนที่**
   - คลิก `Add Item` → `Add Map`
   - วาดกรอบบนหน้ากระดาษ

3. **เพิ่มองค์ประกอบ**
   - **Legend**: `Add Item` → `Add Legend`
   - **Scale Bar**: `Add Item` → `Add Scale Bar`
   - **North Arrow**: `Add Item` → `Add North Arrow`
   - **Title**: `Add Item` → `Add Label`

4. **Export**
   - `Layout` → `Export as Image...` (PNG, JPG)
   - หรือ `Export as PDF...`

---

## 🎯 ตัวอย่างการใช้งานกับไฟล์ของคุณ

### แสดง LST (อุณหภูมิผิวดิน)

```
ไฟล์: Monthly_LST_Filled_2018-2025.tif
Band: เลือก band ที่ต้องการ (1-94)
Color ramp: Reds (แดง = ร้อน, เหลือง = เย็น)
Min: 20°C, Max: 45°C
```

### แสดง NDVI (ดัชนีพืชพรรณ)

```
ไฟล์: Monthly_NDVI_2018-2025.tif
Band: เลือก band ที่ต้องการ
Color ramp: Greens (เขียวเข้ม = พืชพรรณหนาแน่น)
Min: -1, Max: 1
```

### เปรียบเทียบหลายเดือน

1. เปิดไฟล์เดียวกัน 3 ครั้ง
2. แต่ละ layer เลือก band ต่างกัน (เช่น มกราคม, เมษายน, ตุลาคม)
3. จัดเรียงใน Layout แบบ 3 คอลัมน์

---

## ⚠️ ปัญหาที่อาจพบ

### 1. ไฟล์เปิดไม่ได้
- ตรวจสอบว่าไฟล์ไม่เสียหาย
- ลองเปิดด้วย GDAL: `gdalinfo Monthly_LST_Filled_2018-2025.tif`

### 2. แสดงผลช้า
- ไฟล์ขนาดใหญ่ (94 bands) อาจช้า
- ลด resolution ในการแสดงผล
- สร้าง pyramids: คลิกขวา layer → `Properties` → `Pyramids` → `Build Pyramids`

### 3. สีไม่ถูกต้อง
- ปรับ Min/Max values ใหม่
- ใช้ `Cumulative count cut` แทน `Min/Max`

### 4. ไม่มี CRS
- ตั้งค่า CRS: คลิกขวา layer → `Set CRS` → เลือก `EPSG:4326`

---

## 📚 ทรัพยากรเพิ่มเติม

- **QGIS Documentation**: https://docs.qgis.org/
- **QGIS Tutorials**: https://www.qgistutorials.com/
- **YouTube**: ค้นหา "QGIS raster tutorial"

---

## 🚀 เริ่มต้นอย่างรวดเร็ว

**ขั้นตอนพื้นฐาน 3 ขั้น:**

1. **เปิด QGIS** → ลากไฟล์ `.tif` เข้าไป
2. **ดับเบิลคลิก layer** → เลือก `Symbology` → เปลี่ยนสี
3. **Export** → `Project` → `Export Map to Image`

เท่านี้ก็ได้ภาพแผนที่แล้ว! 🎉
