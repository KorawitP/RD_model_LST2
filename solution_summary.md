# สรุปผลการสืบสวน: ทำไม LULC และ DEM มี Feature Importance = 0%

## 🔍 ผลการสืบสวน

### ปัญหาที่พบ
**LULC และ DEM ไม่มีความแปรปรวนในข้อมูล!**

| ตัวแปร | ค่าที่พบ | Unique Values | Std | ปัญหา |
|--------|----------|---------------|-----|-------|
| **LULC** | 0.0 เท่านั้น | 1 | 0.0000 | ❌ ไม่มีความแปรปรวน |
| **DEM** | 101.70 เมตร เท่านั้น | 1 | 0.0000 | ❌ ไม่มีความแปรปรวน |

### เปรียบเทียบกับตัวแปรอื่น

| ตัวแปร | Unique Values | Std | Correlation กับ LST |
|--------|---------------|-----|---------------------|
| Solar_Radiation | 376,267 | 76,361,872 | **0.4244** ✓ |
| Albedo | 46,500 | 25.81 | **0.4006** ✓ |
| NDVI | 957,325 | 0.1446 | -0.0388 ✓ |
| CH4 | 231,734 | 27.76 | 0.0549 ✓ |
| **LULC** | **1** | **0.0000** | **NaN** ❌ |
| **DEM** | **1** | **0.0000** | **NaN** ❌ |

## 💡 สาเหตุ

### สาเหตุหลัก: การสุ่มตัวอย่างพิกเซล

จากโค้ดใน `3.txt`:
```python
SAMPLE_SIZE = 30000  # สุ่ม 30,000 พิกเซล
sampled_indices = np.random.choice(len(valid_pixels), SAMPLE_SIZE, replace=False)
```

**ปัญหา:**
- การสุ่ม 30,000 พิกเซลจากพื้นที่ทั้งประเทศไทยอาจได้พื้นที่ที่มีลักษณะคล้ายกันมาก
- ถ้าพิกเซลที่สุ่มได้อยู่ในพื้นที่เดียวกัน (เช่น พื้นที่ราบภาคกลาง):
  - **LULC** จะเป็นประเภทเดียวกัน (เช่น พื้นที่เกษตร = 0)
  - **DEM** จะมีความสูงใกล้เคียงกัน (เช่น ~101 เมตร)

### สาเหตุรอง: การประมวลผลข้อมูล Static

จากโค้ดใน `3.txt`:
```python
# เติมค่า NaN ด้วย mode และ mean
da_lulc_processed = da_lulc_reindexed.expand_dims(time=time_coords).fillna(lulc_fill_value)
da_dem_processed = da_dem_reindexed.expand_dims(time=time_coords).fillna(dem_fill_value)
```

**ปัญหา:**
- ถ้า reindex ทำให้เกิด NaN มาก การ fillna อาจทำให้ข้อมูลกลายเป็นค่าคงที่

## 🔧 วิธีแก้ไข

### วิธีที่ 1: เพิ่มจำนวนตัวอย่างและปรับวิธีการสุ่ม (แนะนำ)

**แก้ไขใน `3.txt`:**

```python
# แทนที่การสุ่มแบบเดิม
SAMPLE_SIZE = 100000  # เพิ่มจาก 30,000 เป็น 100,000

# หรือใช้ Stratified Sampling ตาม LULC
# เพื่อให้แน่ใจว่าได้ทุกประเภทที่ดิน
```

### วิธีที่ 2: ตรวจสอบข้อมูลต้นทางจาก GEE

**ตรวจสอบว่า:**
1. ไฟล์ `Static_LULC_Thailand.tif` มีหลายประเภทที่ดินหรือไม่?
2. ไฟล์ `Static_DEM_Thailand.tif` มีความสูงที่หลากหลายหรือไม่?

**วิธีตรวจสอบ:**
```python
# โหลดไฟล์ต้นทางโดยตรง
import rasterio
with rasterio.open('Static_LULC_Thailand.tif') as src:
    lulc_data = src.read(1)
    print(f"LULC unique values: {np.unique(lulc_data)}")
    print(f"LULC shape: {lulc_data.shape}")
```

### วิธีที่ 3: ลบ LULC และ DEM ออกจากโมเดล

**เนื่องจากไม่มีความแปรปรวน ควรลบออก:**

```python
# แก้ไขใน train_model_optimized.py
features = ['CH4', 'NO2', 'CO', 'NDVI', 'Albedo', 'Solar_Radiation', 'month', 'year']
# ลบ 'LULC' และ 'DEM' ออก
```

**ข้อดี:**
- โมเดลจะเร็วขึ้น
- ไม่มีตัวแปรที่ไม่มีประโยชน์

**ข้อเสีย:**
- สูญเสียข้อมูลที่อาจมีประโยชน์ถ้าแก้ไขปัญหาได้

### วิธีที่ 4: เทรนโมเดลใหม่โดยไม่ใช้ max_features='sqrt'

**ทดสอบว่าปัญหาเกิดจากพารามิเตอร์หรือไม่:**

```python
model = RandomForestRegressor(
    n_estimators=100,
    max_depth=25,
    min_samples_split=20,
    min_samples_leaf=10,
    max_features=None,  # เปลี่ยนจาก 'sqrt' เป็น None
    random_state=42,
    n_jobs=-1
)
```

**หมายเหตุ:** วิธีนี้จะไม่ช่วยอะไรเพราะปัญหาอยู่ที่ข้อมูล ไม่ใช่พารามิเตอร์

## 📊 ข้อมูลเพิ่มเติม

### ตัวแปรอื่นๆ ที่มีปัญหา

**NO2** ก็มีปัญหาคล้ายกัน:
- Std = 0.0000 (แทบไม่มีความแปรปรวน)
- ค่าอยู่ระหว่าง -0.0000 ถึง 0.0004
- Correlation กับ LST = -0.0201 (ต่ำมาก)

**ข้อเสนอแนะ:** พิจารณาลบ NO2 ออกด้วย หรือตรวจสอบหน่วยของข้อมูล

## ✅ ข้อเสนอแนะสำหรับรายงาน

### เพิ่มในส่วน 3.5.2 ข้อจำกัดของการวิจัย

```markdown
1. **ปัญหาความแปรปรวนของข้อมูล LULC และ DEM**
   - จากการวิเคราะห์พบว่า LULC และ DEM ในชุดข้อมูลที่สุ่มตัวอย่างมีค่าคงที่
   - LULC มีค่าเดียว (0.0) และ DEM มีค่าเดียว (101.70 เมตร)
   - สาเหตุเกิดจากการสุ่มตัวอย่าง 30,000 พิกเซลอาจได้พื้นที่ที่มีลักษณะคล้ายกัน
   - ส่งผลให้โมเดลไม่สามารถเรียนรู้ความสัมพันธ์ของตัวแปรเหล่านี้กับ LST ได้
```

### เพิ่มในส่วน 3.5.3 ข้อเสนอแนะ

```markdown
1. **สำหรับการวิจัยต่อเนื่อง:**
   - ปรับปรุงวิธีการสุ่มตัวอย่างให้ครอบคลุมพื้นที่ที่หลากหลาย
   - ใช้ Stratified Sampling ตามประเภทที่ดิน (LULC) และระดับความสูง (DEM)
   - เพิ่มจำนวนตัวอย่างจาก 30,000 เป็น 100,000 พิกเซลขึ้นไป
   - ตรวจสอบคุณภาพข้อมูลต้นทางจาก Google Earth Engine
```

## 🎯 สรุป

**คำตอบสำหรับคำถาม: "ทำไม LULC และ DEM ถึงมีค่า Feature Importance = 0%?"**

➡️ **เพราะข้อมูล LULC และ DEM ในชุดข้อมูลที่ใช้เทรนไม่มีความแปรปรวน (มีค่าคงที่เพียงค่าเดียว)**

**ไม่ใช่ปัญหาของโมเดลหรือพารามิเตอร์** แต่เป็น**ปัญหาของการเตรียมข้อมูล**โดยเฉพาะขั้นตอนการสุ่มตัวอย่างพิกเซล

**ข้อเสนอแนะหลัก:**
1. ✅ ปรับปรุงวิธีการสุ่มตัวอย่างให้ครอบคลุมพื้นที่ที่หลากหลาย
2. ✅ เพิ่มจำนวนตัวอย่างเป็น 100,000+ พิกเซล
3. ✅ ใช้ Stratified Sampling
4. ⚠️ หรือลบ LULC และ DEM ออกจากโมเดลในระหว่างนี้
