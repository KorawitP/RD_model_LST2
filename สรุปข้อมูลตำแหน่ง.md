# สรุป: การใช้ข้อมูลตำแหน่งในโมเดล LST

## ✅ ผลการตรวจสอบ

### 1. ไฟล์ข้อมูลต้นฉบับ (.tif)
**มีข้อมูลตำแหน่ง ✓**

จากการตรวจสอบไฟล์ `Monthly_Albedo_2018-2025.tif`:
- **ระบบพิกัด**: EPSG:4326 (WGS84 - ระบบพิกัดมาตรฐานสากล)
- **ขนาด**: 924 x 1655 pixels
- **ขอบเขตพื้นที่** (ประเทศไทย):
  - ซ้าย (West): 97.3414°
  - ขวา (East): 105.6419°
  - บน (North): 20.4726°
  - ล่าง (South): 5.6055°
- **ความละเอียด**: 0.01° (~1 km)

### 2. ไฟล์ข้อมูลที่ใช้เทรน (.parquet)
**มีข้อมูลตำแหน่ง ✓**

จากการตรวจสอบ `df_final_processed.parquet`:
- **จำนวนแถว**: 984,274 แถว
- **Columns ที่เกี่ยวข้องกับตำแหน่ง**:
  1. `x` (longitude): 97.4088° ถึง 105.6194°
  2. `y` (latitude): 5.6818° ถึง 20.4412°
  3. `spatial_ref`: ข้อมูลระบบพิกัด

### 3. โมเดล Random Forest
**ไม่ได้ใช้ข้อมูลตำแหน่งโดยตรง ✗**

Features ที่ใช้ในโมเดล (10 ตัว):
1. CH4 (มีเทน)
2. NO2 (ไนโตรเจนไดออกไซด์)
3. CO (คาร์บอนมอนอกไซด์)
4. LULC (การใช้ที่ดิน)
5. DEM (ความสูง)
6. NDVI (ดัชนีพืชพรรณ)
7. Albedo (ค่าสะท้อนแสง)
8. Solar_Radiation (รังสีดวงอาทิตย์)
9. month (เดือน)
10. year (ปี)

**ไม่มี `x` และ `y` ในรายการ features**

---

## 📊 สรุปสถานะ

| ส่วนประกอบ | มีข้อมูลตำแหน่ง | ใช้ในโมเดล |
|-----------|----------------|-----------|
| ไฟล์ .tif ต้นฉบับ | ✓ มี | - |
| ไฟล์ .parquet | ✓ มี (x, y) | ✗ ไม่ใช้ |
| โมเดล Random Forest | - | ✗ ไม่ใช้ |

---

## 💡 คำอธิบาย

### ทำไมโมเดลไม่ใช้ x, y โดยตรง?

แม้ข้อมูลจะมีพิกัด (x, y) แต่โมเดลไม่ได้ใช้โดยตรง เพราะ:

1. **โมเดลเรียนรู้จากลักษณะของข้อมูล ไม่ใช่ตำแหน่ง**
   - ใช้ DEM (ความสูง) แทนการใช้ตำแหน่ง
   - ใช้ LULC (การใช้ที่ดิน) แทนการระบุพื้นที่
   
2. **ต้องการให้โมเดลสามารถนำไปใช้ได้หลากหลาย**
   - ถ้าใช้ x, y โมเดลจะจำตำแหน่งเฉพาะ
   - ไม่สามารถนำไปใช้กับพื้นที่อื่นได้

3. **ข้อมูลยังคงมีความหมายเชิงพื้นที่**
   - DEM บ่งบอกว่าเป็นภูเขาหรือที่ราบ
   - LULC บ่งบอกว่าเป็นเมือง ป่า หรือนา
   - ค่าต่างๆ มาจากตำแหน่งเฉพาะในประเทศไทย

---

## 🎯 ข้อดี-ข้อเสีย

### ✅ ข้อดีของการไม่ใช้ x, y
- โมเดลเรียนรู้จากความสัมพันธ์ของตัวแปร ไม่ใช่ตำแหน่ง
- สามารถนำไปใช้กับพื้นที่อื่นที่มีลักษณะคล้ายกันได้
- ไม่เกิด overfitting กับตำแหน่งเฉพาะ

### ⚠️ ข้อเสียของการไม่ใช้ x, y
- อาจพลาดรูปแบบเชิงพื้นที่บางอย่าง
- ไม่สามารถจับ spatial autocorrelation (ความสัมพันธ์เชิงพื้นที่)
- อาจมีความแม่นยำต่ำกว่าในบางพื้นที่

---

## 🔧 คำแนะนำในการปรับปรุง

หากต้องการเพิ่มความแม่นยำ อาจลองเพิ่ม features เชิงพื้นที่:

### 1. ใช้ตำแหน่งโดยตรง
```python
features = ['x', 'y', 'CH4', 'NO2', 'CO', ...]
```

### 2. สร้าง features เชิงพื้นที่ใหม่
```python
# ระยะห่างจากชายฝั่ง
df['distance_to_coast'] = calculate_distance_to_coast(df['x'], df['y'])

# ระยะห่างจากเมืองใหญ่
df['distance_to_bangkok'] = calculate_distance(df['x'], df['y'], bangkok_coords)

# ภูมิภาค (เหนือ กลาง ใต้ ฯลฯ)
df['region'] = assign_region(df['x'], df['y'])
```

### 3. ใช้ Spatial Features Engineering
```python
# Spatial lag (ค่าเฉลี่ยของพื้นที่ใกล้เคียง)
df['LST_spatial_lag'] = calculate_spatial_lag(df, 'LST')

# Spatial clustering
df['cluster'] = spatial_clustering(df[['x', 'y']])
```

---

## 📝 สรุปสุดท้าย

**ข้อมูลที่ใช้งานมีข้อมูลตำแหน่ง (x, y) อยู่แล้ว** แต่โมเดลปัจจุบันไม่ได้ใช้โดยตรง

- ✅ **ไฟล์ .tif**: มีข้อมูล georeferencing ครบถ้วน
- ✅ **ไฟล์ .parquet**: มี columns x, y พร้อมใช้งาน
- ⚠️ **โมเดล**: ไม่ได้ใช้ x, y ในการเทรน

หากต้องการใช้ข้อมูลตำแหน่ง เพียงแค่เพิ่ม `'x'` และ `'y'` เข้าไปใน list `features` ในไฟล์ `train_model_*.py`
