# ทำไมภาพไม่ละเอียด (Pixelated)?

## 🔍 สาเหตุหลัก

### 1. **ความละเอียดของข้อมูลต้นฉบับต่ำ**
จากการตรวจสอบไฟล์:
- **ขนาดภาพ**: 924 x 1655 pixels
- **พื้นที่ครอบคลุม**: ประเทศไทยทั้งหมด
- **ความละเอียดจริง**: ~1 km ต่อ pixel

**คำนวณ:**
```
ประเทศไทย:
- กว้าง: 97.34° ถึง 105.64° = 8.3°
- สูง: 5.61° ถึง 20.47° = 14.86°

ความละเอียด:
- แนวนอน: 8.3° / 924 pixels = 0.009° ≈ 1 km/pixel
- แนวตั้ง: 14.86° / 1655 pixels = 0.009° ≈ 1 km/pixel
```

**หมายความว่า:**
- 1 pixel = พื้นที่ประมาณ 1 km x 1 km
- ประเทศไทยทั้งประเทศถูกบีบอัดให้เหลือแค่ 924 x 1655 pixels
- เมื่อซูมเข้าไปดู จะเห็นเป็นก้อนๆ (pixelated)

---

### 2. **การตั้งค่า Export จาก Google Earth Engine**

ดูจากไฟล์ `GEE_Export_Additional_Data.js`:
```javascript
var exportParams = {
  scale: 1000,  // 1 km resolution ← นี่คือสาเหตุ!
  ...
};
```

**ความละเอียดที่ใช้:**
- **scale: 1000** = 1,000 เมตร = 1 กิโลเมตร ต่อ pixel

**เปรียบเทียบความละเอียด:**
| ความละเอียด | เมตร/pixel | เหมาะสำหรับ |
|------------|-----------|------------|
| **ต่ำมาก** | 10,000 m | ภาพรวมทวีป |
| **ต่ำ** | 1,000 m (ปัจจุบัน) | ภาพรวมประเทศ |
| **ปานกลาง** | 500 m | ภูมิภาค/จังหวัด |
| **ดี** | 250 m | เมือง/อำเภอ |
| **ละเอียด** | 100 m | ชุมชน/ตำบล |
| **ละเอียดมาก** | 30 m | อาคาร/ถนน |
| **ละเอียดสูงสุด** | 10 m | บ้าน/ต้นไม้ |

---

### 3. **ข้อจำกัดของข้อมูล MODIS**

ข้อมูล LST ที่คุณใช้มาจาก **MODIS**:
- ความละเอียดดั้งเดิม: **1 km** (ไม่สามารถละเอียดกว่านี้ได้)
- ถ้าต้องการละเอียดกว่า ต้องใช้ดาวเทียมอื่น

**ตัวเลือกดาวเทียมตามความละเอียด:**
| ดาวเทียม | ความละเอียด LST | ข้อดี | ข้อเสีย |
|---------|----------------|-------|--------|
| **MODIS** | 1 km | ฟรี, ข้อมูลยาวนาน | ละเอียดต่ำ |
| **Landsat 8/9** | 100 m (resampled) | ฟรี, ละเอียดกว่า | 16 วันต่อครั้ง |
| **Sentinel-3** | 1 km | ฟรี, ทุกวัน | เหมือน MODIS |
| **ASTER** | 90 m | ละเอียด | ไม่ฟรี, ข้อมูลน้อย |
| **ECOSTRESS** | 70 m | ละเอียดมาก | ข้อมูลไม่ต่อเนื่อง |

---

## 🔧 วิธีแก้ไข

### วิธีที่ 1: เพิ่มความละเอียดในการ Export (แนะนำ)

**แก้ไขไฟล์ GEE Script:**
```javascript
// เดิม
var exportParams = {
  scale: 1000,  // 1 km
  ...
};

// ใหม่ (ถ้าต้องการละเอียดกว่า)
var exportParams = {
  scale: 500,   // 500 m (ละเอียดขึ้น 2 เท่า)
  // หรือ
  scale: 250,   // 250 m (ละเอียดขึ้น 4 เท่า)
  ...
};
```

**⚠️ คำเตือน:**
- ความละเอียดสูง = ไฟล์ใหญ่ขึ้นมาก
- scale: 500m → ไฟล์ใหญ่ขึ้น 4 เท่า
- scale: 250m → ไฟล์ใหญ่ขึ้น 16 เท่า
- อาจเกิน Google Drive quota

---

### วิธีที่ 2: ใช้ Interpolation (ทำให้ดูเรียบขึ้น)

**แก้ไขสคริปต์ Python:**
```python
# เพิ่ม interpolation ในการแสดงผล
im = ax.imshow(data, 
               cmap=cmap, 
               vmin=vmin, 
               vmax=vmax, 
               aspect='auto',
               interpolation='bilinear')  # ← เพิ่มบรรทัดนี้
```

**ตัวเลือก interpolation:**
- `'nearest'` (default) - เห็นเป็นก้อนๆ ชัดเจน
- `'bilinear'` - เรียบขึ้น แต่ยังเห็นขอบ
- `'bicubic'` - เรียบมาก แต่อาจเบลอ
- `'lanczos'` - คุณภาพดีที่สุด แต่ช้า

---

### วิธีที่ 3: Export เฉพาะพื้นที่ที่สนใจ

แทนที่จะ export ทั้งประเทศ ให้ export เฉพาะพื้นที่:

```javascript
// เดิม - ทั้งประเทศไทย
var thailand = ee.FeatureCollection('USDOS/LSIB_SIMPLE/2017')
  .filter(ee.Filter.eq('country_na', 'Thailand'));
var geometry = thailand.geometry();

// ใหม่ - เฉพาะกรุงเทพฯ (ตัวอย่าง)
var geometry = ee.Geometry.Rectangle([
  100.3, 13.5,  // [lon_min, lat_min]
  100.9, 14.0   // [lon_max, lat_max]
]);
```

**ผลลัพธ์:**
- พื้นที่เล็กลง → ใช้ scale เดิม แต่ละเอียดขึ้น
- หรือใช้ scale ต่ำกว่า (เช่น 100m) ได้

---

### วิธีที่ 4: ใช้ดาวเทียมความละเอียดสูงกว่า

**ใช้ Landsat 8/9 แทน MODIS:**
```javascript
// Landsat 8 - ความละเอียด 30m (แต่ LST ต้อง resample เป็น 100m)
var landsat = ee.ImageCollection('LANDSAT/LC08/C02/T1_L2')
  .filterDate(startDate, endDate)
  .filterBounds(geometry);

// คำนวณ LST จาก Landsat
var lst = landsat.map(function(img) {
  // ... คำนวณ LST จาก thermal band
});
```

**ข้อดี:**
- ความละเอียด 100m (ละเอียดกว่า MODIS 10 เท่า)

**ข้อเสีย:**
- ถ่ายภาพทุก 16 วัน (ไม่ได้ทุกวัน)
- ต้องคำนวณ LST เอง (ซับซ้อนกว่า)

---

## 📊 เปรียบเทียบขนาดไฟล์

| ความละเอียด | Pixels | ขนาดไฟล์ (ประมาณ) | เวลา Export |
|------------|--------|------------------|------------|
| 1000m (ปัจจุบัน) | 924 x 1655 | ~150 MB | 5-10 นาที |
| 500m | 1848 x 3310 | ~600 MB | 15-30 นาที |
| 250m | 3696 x 6620 | ~2.4 GB | 1-2 ชั่วโมง |
| 100m | 9240 x 16550 | ~15 GB | หลายชั่วโมง |

---

## 💡 คำแนะนำ

### สำหรับงานวิจัยระดับประเทศ:
✅ **ใช้ 1 km** (ปัจจุบัน) - เหมาะสมแล้ว
- ดูภาพรวมทั้งประเทศ
- ไฟล์ไม่ใหญ่เกินไป
- ประมวลผลเร็ว

### สำหรับงานวิจัยระดับภูมิภาค/จังหวัด:
✅ **ใช้ 500m** - สมดุลดี
- ละเอียดพอดู pattern
- ไฟล์ยังจัดการได้

### สำหรับงานวิจัยระดับเมือง:
✅ **ใช้ 250m หรือ Landsat (100m)**
- เห็นรายละเอียดชัดเจน
- เหมาะกับพื้นที่เล็ก

---

## 🎯 สรุป

**ภาพไม่ละเอียดเพราะ:**
1. ข้อมูล MODIS มีความละเอียดแค่ 1 km ต่อ pixel
2. Export ด้วย scale: 1000m
3. ประเทศไทยทั้งประเทศ = แค่ 924 x 1655 pixels

**นี่ไม่ใช่ปัญหา แต่เป็นข้อจำกัดของข้อมูล!**

ถ้าต้องการละเอียดกว่านี้:
- ใช้ scale ต่ำกว่า (500m, 250m)
- หรือเปลี่ยนไปใช้ Landsat (100m)
- หรือ export เฉพาะพื้นที่ที่สนใจ
