# --- การเทรนโมเดล Hybrid (ลูกผสม Random Forest + Deep Learning 🧠🌲) ---
import joblib
import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.impute import SimpleImputer
from sklearn.ensemble import RandomForestRegressor
from sklearn.neural_network import MLPRegressor
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import mean_squared_error, r2_score
import time
import gc
import os

# ==============================================================================
# 📝 คำอธิบายสำหรับมือใหม่ (Concept):
# นี่คือโมเดล "ลูกผสม" (Hybrid / Ensemble) ที่พยายามเอาข้อดีของ 2 โลกมารวมกัน:
#
# 1. Random Forest (RF): เก่งเรื่องข้อมูลตาราง, ไม่ต้องปรับแต่งเยอะ, ทนทาน (Robust)
# 2. Deep Learning (DNN): เก่งเรื่องความซับซ้อน, เรียนรู้ความสัมพันธ์ลึกซึ้งได้ (Complex Patterns)
#
# หลักการทำงาน (Weighted Average Ensemble):
# - ให้ทั้งคู่ลองทำนายผลออกมาคนละชุด
# - เอาคำตอบของทั้งคู่มา "ผสมกัน" โดยการถ่วงน้ำหนัก (Weighting)
# - ใครเก่งกว่า (R² เยอะกว่า) จะได้น้ำหนักความเชื่อถือเยอะกว่า
# - ผลลัพธ์สุดท้าย คือค่าเฉลี่ยแบบถ่วงน้ำหนักของทั้งคู่
# ==============================================================================

print("="*70)
print("=== Hybrid Model Training (RF + Deep Learning) ===")
print("=== ระบบลูกผสม: ป่าไม้ (RF) + สมองกล (Neural Network) ===")
print("="*70)

# ------------------------------------------------------------------------------
# 1. โหลดข้อมูล
# ------------------------------------------------------------------------------
print("\n[1/6] กำลังโหลดข้อมูล...")
start_time = time.time()
if os.path.exists('data/df_final_processed.parquet'):
    df = pd.read_parquet('data/df_final_processed.parquet')
else:
    print("❌ ไม่พบไฟล์ข้อมูล! กรุณารัน train_model_final.py ก่อน")
    exit()
    
print(f"✓ โหลดสำเร็จ: {len(df):,} แถว ({time.time()-start_time:.2f} วินาที)")

# ------------------------------------------------------------------------------
# 2. เตรียม features
# ------------------------------------------------------------------------------
features = ['CH4', 'NO2', 'CO', 'NDVI', 'Albedo', 'Solar_Radiation', 'month', 'year']
target = 'LST'

# แปลงเป็น Numpy Array เพื่อความเร็วในการคำนวณของ Neural Network
X = df[features].values
y = df[target].values.ravel()  # ravel() ทำให้เป็น array 1 มิติ

del df
gc.collect()

# ------------------------------------------------------------------------------
# 3. Preprocessing (การเตรียมข้อมูลขั้นสูง)
# ------------------------------------------------------------------------------
print("\n[2/6] Preprocessing (เตรียมความพร้อม)...")

# 3.1 Imputation (เติมค่าที่หายไป)
print("   > Imputation: เติมค่าว่างด้วยค่าเฉลี่ย")
imputer = SimpleImputer(strategy='mean')
X = imputer.fit_transform(X)

# 3.2 Scaling (จำเป็นมากสำหรับ Neural Network!)
# Deep Learning จะทำงานได้แย่มากถ้าข้อมูลแต่ละตัวมีหน่วยต่างกัน (เช่น 0.01 vs 1000)
# StandardScaler จะปรับข้อมูลทุกตัวให้มีหน่วยกลาง (z-score) คือ Mean=0, Std=1
print("   > Scaling: ปรับมาตรฐานข้อมูล (สำคัญมากสำหรับ Deep Learning)")
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)

# แบ่งข้อมูล
X_train, X_test, y_train, y_test = train_test_split(X_scaled, y, test_size=0.3, random_state=42)
print(f"✓ แบ่งข้อมูล: Train={len(X_train):,}, Test={len(X_test):,}")

# ------------------------------------------------------------------------------
# 4. สร้างและเทรนโมเดล (Training)
# ------------------------------------------------------------------------------
print("\n[3/6] เริ่มต้นเทรนโมเดล (เริ่มเรียนรู้)...")

# --- Model 1: Random Forest (RF) ---
print("\n👉 โมเดลที่ 1: Random Forest (ป่าไม้ตัดสินใจ)")
print("   ...กำลังเทรน Random Forest...")
rf_model = RandomForestRegressor(
    n_estimators=100,
    max_depth=20,  # ลดความลึกนิดนึงเพื่อให้ทำงานเร็วขึ้น
    min_samples_split=20,
    n_jobs=-1,
    random_state=42
)
rf_model.fit(X_train, y_train)
rf_pred = rf_model.predict(X_test)
rf_r2 = r2_score(y_test, rf_pred)
print(f"   ✅ RF Test Result (R²): {rf_r2:.4f}")

# --- Model 2: Deep Neural Network (DNN) ---
print("\n👉 โมเดลที่ 2: Deep Neural Network (สมองกล Multi-Layer Perceptron)")
print("   โครงสร้าง: Input -> 128 -> 64 -> 32 -> Output (3 Hidden Layers)")
print("   ...กำลังเทรน DNN (อาจใช้เวลานานกว่า RF)...")

dnn_model = MLPRegressor(
    hidden_layer_sizes=(128, 64, 32), # จำนวนเซลล์สมองในแต่ละชั้น
    activation='relu',                # ฟังก์ชันกระตุ้น (ReLU เป็นมาตรฐานนิยม)
    solver='adam',                    # ตัวช่วยปรับจูน (Optimizer)
    alpha=0.0001,                     # ค่าปรับลดความซับซ้อน (L2 Regularization)
    batch_size=64,                    # เรียนทีละ 64 ตัวอย่าง
    max_iter=500,                     # วนซ้ำสูงสุด 500 รอบ
    early_stopping=True,              # 🛑 หยุดเองถ้าความฉลาดไม่เพิ่ม (กันเสียเวลา)
    random_state=42
)
dnn_model.fit(X_train, y_train)
dnn_pred = dnn_model.predict(X_test)
dnn_r2 = r2_score(y_test, dnn_pred)
print(f"   ✅ DNN Test Result (R²): {dnn_r2:.4f}")

# ------------------------------------------------------------------------------
# 5. สร้างทีมผสม (Ensemble / Hybrid)
# ------------------------------------------------------------------------------
print("\n[4/6] สร้างทีมผสม (Hybrid Ensemble)...")
print("   หลักการ: เอาคะแนน R² มาเป็นตัวกำหนดน้ำหนัก (ใครเก่งกว่าเชื่อคนนั้นเยอะกว่า)")

# คำนวณน้ำหนัก (Weights)
total_score = rf_r2 + dnn_r2
w_rf = rf_r2 / total_score     # น้ำหนักของ RF
w_dnn = dnn_r2 / total_score   # น้ำหนักของ DNN

print(f"   ⚖️  น้ำหนักที่คำนวณได้:")
print(f"       - เชื่อ Random Forest: {w_rf*100:.2f}%")
print(f"       - เชื่อ Deep Learning: {w_dnn*100:.2f}%")

# รวมคำตอบ (Weighted Average Prediction)
# สูตร: (คำตอบRF * น้ำหนักRF) + (คำตอบDNN * น้ำหนักDNN)
y_pred_ensemble = (rf_pred * w_rf) + (dnn_pred * w_dnn)

ensemble_r2 = r2_score(y_test, y_pred_ensemble)
ensemble_rmse = np.sqrt(mean_squared_error(y_test, y_pred_ensemble))

# ------------------------------------------------------------------------------
# 6. สรุปผลการแข่งขันและทีมผสม
# ------------------------------------------------------------------------------
print("\n" + "="*70)
print("=== 🏁 FINAL SCOREBOARD (ตารางคะแนน) ===")
print("="*70)
print(f"1. 🌲 Random Forest R² : {rf_r2:.4f}")
print(f"2. 🧠 Deep Learning R² : {dnn_r2:.4f}")
print("-" * 30)
print(f"🏆 🤝 Hybrid Ensemble R²: {ensemble_r2:.4f} (ผลลัพธ์ทีมผสม)")
print(f"📉 Hybrid Ensemble RMSE: +/- {ensemble_rmse:.4f} องศา")
print("="*70)

if ensemble_r2 > max(rf_r2, dnn_r2):
    print("✨ SUCCESS: ทีมผสมทำงานได้ดีกว่าแยกกันทำ! (Synergy Effect)")
else:
    print("ℹ️ Note: ทีมผสมอาจไม่ได้คะแนนพุ่งปรี๊ดขึ้น")
    print("   แต่ข้อดีคือ 'ความเสถียร' (Stability) เพราะช่วยกันเฉลี่ยความผิดพลาด")

# ------------------------------------------------------------------------------
# 7. บันทึกผล
# ------------------------------------------------------------------------------
print("\n[6/6] บันทึกโมเดลทั้งหมด...")

# บันทึกแยกชิ้นส่วน
joblib.dump(rf_model, 'models/model_rf.joblib')
joblib.dump(dnn_model, 'models/model_dnn.joblib')
joblib.dump(scaler, 'models/scaler.joblib') # อย่าลืม Scaler! (จำเป็นตอนเอาไปใช้จริง)

# บันทึกค่าน้ำหนักใส่ Text file จะได้รู้ว่าตอนเอาไปใช้ต้องถ่วงน้ำหนักเท่าไหร่
with open("ensemble_weights.txt", "w") as f:
    f.write(f"RF:{w_rf}\nDNN:{w_dnn}")
    
print("✓ บันทึกโมเดลและ Scaler เรียบร้อย")
print("🎉 เสร็จสิ้นกระบวนการ!")
