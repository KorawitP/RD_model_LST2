# --- การเทรนโมเดล Random Forest (เวอร์ชันแก้ Overfitting 🛡️) ---
import joblib
import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split, cross_val_score
from sklearn.impute import SimpleImputer
from sklearn.ensemble import RandomForestRegressor
from sklearn.metrics import mean_squared_error, r2_score
import time
import gc

# ==============================================================================
# 📝 คำอธิบายสำหรับมือใหม่ (Concept):
# ไฟล์นี้ "ฉลาดกว่า" ไฟล์ fast.py เพราะมันมีการป้องกันการ "จำข้อสอบ" (Overfitting)
#
# Overfitting คืออะไร?
# คือการที่ AI ท่องจำข้อมูลที่เรียนมาเป๊ะๆ จนทำนายของเก่าได้แม่นมาก (R² สูงปรี๊ด)
# แต่พอเจอข้อมูลใหม่ (Test Set) กลับทำนายผิดเยอะ (R² ตกฮวบ)
#
# วิธีแก้ในไฟล์นี้:
# เราจะ "จำกัดความสามารถ" ของต้นไม้ไม่ให้ซับซ้อนเกินไป (Pruning)
# เช่น ห้ามต้นไม้สูงเกิน 25 ชั้น, ห้ามแตกกิ่งถ้าข้อมูลน้อยเกินไป
# เพื่อบังคับให้ AI เข้าใจ "หลักการภาพรวม" แทนการท่องจำรายละเอียดเล็กน้อย
# ==============================================================================

print("="*60)
print("=== การเทรนโมเดล Random Forest (เวอร์ชันแก้ Overfitting) ===")
print("="*60)

# ------------------------------------------------------------------------------
# 1. โหลดข้อมูล
# ------------------------------------------------------------------------------
print("\n[1/7] กำลังโหลดข้อมูล...")
start_time = time.time()
df = pd.read_parquet('data/df_final_processed.parquet')
print(f"✓ โหลดข้อมูลสำเร็จ: {len(df):,} แถว ({time.time()-start_time:.2f} วินาที)")

# ------------------------------------------------------------------------------
# 2. เตรียม features และ target
# ------------------------------------------------------------------------------
print("\n[2/7] กำลังเตรียมข้อมูล...")
features = ['CH4', 'NO2', 'CO', 'LULC', 'DEM', 'NDVI', 'Albedo', 'Solar_Radiation', 'month', 'year']
target = 'LST'

X_raw = df[features].copy()
y = df[target].copy()
del df
gc.collect()

print(f"✓ Features: {len(features)} ตัว")
print(f"✓ Target: {target}")

# ------------------------------------------------------------------------------
# 3. ทำ Imputation (เติมค่าที่หายไป)
# ------------------------------------------------------------------------------
print("\n[3/7] กำลังเติมค่าที่หายไป (Imputation)...")
start_time = time.time()
imputer = SimpleImputer(strategy='mean')
X = imputer.fit_transform(X_raw)
del X_raw
gc.collect()
print(f"✓ เสร็จสิ้น ({time.time()-start_time:.2f} วินาที)")

# ------------------------------------------------------------------------------
# 4. แบ่งข้อมูล (Train/Test Split)
# ------------------------------------------------------------------------------
print("\n[4/7] กำลังแบ่งข้อมูล Train/Test...")
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=42)
del X, y
gc.collect()
print(f"✓ แบ่ง Train: {len(X_train):,}, Test: {len(X_test):,}")

# ------------------------------------------------------------------------------
# 5. เทรนโมเดลแบบปรับแต่งค่า (Hyperparameter Tuning Breakdown)
# ------------------------------------------------------------------------------
print("\n[5/7] กำลังเทรนโมเดลด้วยพารามิเตอร์พิเศษ...")
print("🛡️ มาตรการป้องกันการจำข้อสอบ (Overfitting Prevention):")
print("   1. max_depth=25         -> ห้ามต้นไม้สูงเกิน 25 ชั้น (กันจำรายละเอียดถยิบย่อย)")
print("   2. min_samples_split=20 -> ถ้าข้อมูลในกิ่งเหลือน้อยกว่า 20 ไม่ต้องแตกกิ่งต่อแล้ว (พอแค่นั้น)")
print("   3. min_samples_leaf=10  -> ปลายกิ่งต้องมีข้อมูลอย่างน้อย 10 ตัว (ห้ามสร้างกิ่งเพื่อข้อมูลตัวเดียว)")
print("   4. max_features='sqrt'  -> แต่ละต้นไม้ให้ดูตัวแปรแค่บางส่วน (สุ่มรากที่สองของจำนวนตัวแปร)")
print("\n...กำลังประมวลผล (อาจใช้นานกว่าปกติเล็กน้อย)...")

start_time = time.time()

model = RandomForestRegressor(
    n_estimators=100,         # จำนวนต้นไม้ 100 ต้น
    max_depth=25,             # 🛡️ จำกัดความลึก
    min_samples_split=20,     # 🛡️ ต้องมีข้อมูลพอถึงจะแยก
    min_samples_leaf=10,      # 🛡️ ปลายกิ่งต้องมีข้อมูลพอ
    max_features='sqrt',      # 🛡️ ดูตัวแปรไม่ครบทุกตัว (บังคับให้มองมุมใหม่ๆ)
    n_jobs=-1,                # ใช้ทุก Core
    random_state=42,
    verbose=1
)

model.fit(X_train, y_train)
training_time = time.time() - start_time

print(f"\n✓ เทรนเสร็จสิ้น! ใช้เวลา: {training_time/60:.2f} นาที")

# ------------------------------------------------------------------------------
# 6. Cross-Validation (การตรวจสอบย้อนกลับ)
# ------------------------------------------------------------------------------
print("\n[6/7] กำลังทำ Cross-Validation (5-Fold)...")
# Concept: เราจะแบ่งข้อมูล Train เป็น 5 ส่วน แล้ววนกันสอบ 5 รอบ
# รอบที่ 1: ใช้ส่วน 1-4 เรียน, สอบส่วน 5
# รอบที่ 2: ใช้ส่วน 1,2,3,5 เรียน, สอบส่วน 4
# ... ทำจนครบ
# วิธีนี้จะให้คะแนนที่ "น่าเชื่อถือที่สุด" ว่าโมเดลเก่งจริงไหม (ไม่ใช่ฟลุ๊ค)
print("โปรดรอสักครู่... (กำลังวนสอบ 5 รอบ)")
cv_scores = cross_val_score(model, X_train, y_train, cv=5, 
                            scoring='r2', n_jobs=-1, verbose=0)
print(f"✓ คะแนนดิบ 5 รอบ: {cv_scores}")
print(f"✓ คะแนนเฉลี่ย (Mean R²): {cv_scores.mean():.4f}")

# ------------------------------------------------------------------------------
# 7. ประเมินผล (Evaluation & Gap Analysis)
# ------------------------------------------------------------------------------
print("\n[7/7] กำลังประเมินผลและเช็ค Overfitting...")
y_pred_train = model.predict(X_train)
y_pred_test = model.predict(X_test)

r2_train = r2_score(y_train, y_pred_train)
r2_test = r2_score(y_test, y_pred_test)
rmse_test = np.sqrt(mean_squared_error(y_test, y_pred_test))

print("\n" + "="*60)
print("=== 📊 ผลการประเมินโมเดล ===")
print("="*60)
print(f"Train R²: {r2_train:.4f} (ผลสอบในห้องเรียน)")
print(f"Test  R²: {r2_test:.4f} (ผลสอบจริง)")
print(f"Test RMSE: {rmse_test:.4f} องศา")

# ตรวจสอบส่วนต่าง (Gap)
gap = r2_train - r2_test
print(f"\n📉 Overfitting Gap (ส่วนต่างคะแนน): {gap:.4f}")
# ถ้า Gap น้อย แสดงว่า ผลสอบในห้องเรียนกับสอบจริงใกล้เคียงกัน (ดีมาก)
if gap < 0.1:
    print(f"  ✅ ดีเยี่ยม! โมเดลไม่ค่อยจำข้อสอบ (Overfitting น้อย)")
elif gap < 0.2:
    print(f"  ⚠️ ปานกลาง - เริ่มมีการจำข้อสอบบ้าง")
else:
    print(f"  ❌ แย่ - โมเดลจำข้อสอบมาตอบ (Overfitting สูง)")
print("="*60)

# ------------------------------------------------------------------------------
# 8. บันทึกผล
# ------------------------------------------------------------------------------
print("\n[8/8] กำลังบันทึกโมเดล...")
model_path = 'models/random_forest_model_optimized.joblib'
joblib.dump(model, model_path)
print(f"✓ บันทึกที่: {model_path}")

# แสดง Feature Importance
print("\n=== ปัจจัยที่มีผลมากที่สุด (Top 10) ===")
feature_importance = pd.DataFrame({
    'Feature': features,
    'Importance': model.feature_importances_
}).sort_values('Importance', ascending=False)
print(feature_importance.to_string(index=False))

print("\n" + "="*60)
print("=== เสร็จสิ้นทั้งหมด! ===")
print("="*60)
print("\n💡 สรุปความเปลี่ยนแปลง:")
print("โมเดลนี้อาจจะได้คะแนน Train น้อยกว่า fast.py (เพราะเราห้ามมันจำ)")
print("แต่คะแนน Test (สอบจริง) ควรจะใกล้เคียงกัน และ Gap ควรจะลดลง")
print("ซึ่งแปลว่าโมเดลนี้ 'เสถียร' กว่าในการนำไปใช้งานจริง")
