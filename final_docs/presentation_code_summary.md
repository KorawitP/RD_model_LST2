# 📋 สรุปไฟล์โค้ดสำหรับนำเสนอ

> **สร้างเมื่อ:** 2026-02-02  
> **วัตถุประสงค์:** เตรียมตัวนำเสนอ โดยเน้นอธิบาย Preprocessing

---

## 🗂️ โครงสร้างไฟล์ทั้งหมด

```
RD_model_LST2/
├── 📊 DATA COLLECTION
│   ├── GEE_Export_Additional_Data.js    # Script ดึงข้อมูลจาก Google Earth Engine
│   └── data/                            # โฟลเดอร์เก็บ GeoTIFF files
│       ├── Monthly_LST_Filled_2018-2025.tif
│       ├── Monthly_CH4_2018-2025.tif
│       ├── Monthly_NO2_2018-2025.tif
│       ├── Monthly_CO_2018-2025.tif
│       └── ...
│
├── 🔧 PREPROCESSING & TRAINING
│   ├── train_model_fast.py              # เวอร์ชันเร่งความเร็ว (พื้นฐาน)
│   ├── train_model_optimized.py         # เวอร์ชันแก้ Overfitting
│   ├── train_model_final.py             # เวอร์ชันสุดท้าย (ตัด LULC/DEM)
│   └── train_hybrid_model.py            # Hybrid Model (RF + DNN)
│
├── 📈 EVALUATION & ANALYSIS
│   ├── evaluate_model.py                # ประเมินประสิทธิภาพโมเดล
│   ├── investigate_features.py          # สืบสวนปัญหา LULC/DEM
│   ├── create_heatmap.py                # สร้าง Correlation Heatmap
│   └── test_model_stability.py          # ทดสอบความเสถียรของโมเดล
│
├── 📦 OUTPUT FILES
│   ├── df_final_processed.parquet       # ข้อมูลที่ผ่าน preprocessing แล้ว
│   ├── model_rf.joblib                  # Random Forest Model
│   ├── model_dnn.joblib                 # Deep Neural Network Model
│   ├── scaler.joblib                    # StandardScaler สำหรับ DNN
│   └── ensemble_weights.txt             # ค่าน้ำหนัก Ensemble
│
└── 📄 DOCUMENTATION
    └── final_docs/                      # เอกสารวิจัย
        └── thesis_combined.md           # เอกสารฉบับรวม
```

---

## 🎯 ไฟล์สำคัญที่ต้องอธิบาย (เรียงตามลำดับ Pipeline)

---

### 1️⃣ `GEE_Export_Additional_Data.js` - Data Collection

**ตำแหน่ง:** Root folder  
**หน้าที่:** ดึงข้อมูลจาก Google Earth Engine

```javascript
// ตัวอย่างโค้ดสำคัญ
var thailand = ee
  .FeatureCollection("USDOS/LSIB_SIMPLE/2017")
  .filter(ee.Filter.eq("country_na", "Thailand"));

var startDate = "2018-01-01";
var endDate = "2025-10-31";

// Export ความละเอียด 1 km
var exportParams = {
  scale: 1000, // 1 km resolution
  crs: "EPSG:4326",
};
```

**ข้อมูลที่ดึง:**
| แหล่งข้อมูล | ตัวแปร |
|------------|--------|
| Sentinel-5P | CH₄, NO₂, CO |
| MODIS | LST, NDVI, Albedo |
| ERA5 | Solar Radiation |

---

### 2️⃣ `train_hybrid_model.py` - Preprocessing & Training (⭐ ไฟล์หลัก)

**ตำแหน่ง:** Root folder  
**หน้าที่:** เตรียมข้อมูล + ฝึกสอนโมเดล

#### 📌 ขั้นตอน Preprocessing ในโค้ด:

```python
# --- ขั้นตอนที่ 1: โหลดข้อมูล ---
df = pd.read_parquet('df_final_processed.parquet')

# --- ขั้นตอนที่ 2: เลือก Features ---
features = ['CH4', 'NO2', 'CO', 'NDVI', 'Albedo', 'Solar_Radiation', 'month', 'year']
# หมายเหตุ: ตัด LULC และ DEM ออกเพราะ Zero Variance

# --- ขั้นตอนที่ 3: Imputation ---
from sklearn.impute import SimpleImputer
imputer = SimpleImputer(strategy='mean')  # ใช้ค่าเฉลี่ยเติมค่าที่หายไป
X = imputer.fit_transform(X)

# --- ขั้นตอนที่ 4: Standardization (สำหรับ DNN เท่านั้น) ---
from sklearn.preprocessing import StandardScaler
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)

# --- ขั้นตอนที่ 5: Data Splitting ---
from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(
    X_scaled, y,
    test_size=0.3,      # 70% train, 30% test
    random_state=42     # เพื่อให้ผลลัพธ์ทำซ้ำได้
)
```

#### 📌 การสร้างโมเดล:

```python
# --- Model 1: Random Forest ---
rf_model = RandomForestRegressor(
    n_estimators=100,      # จำนวนต้นไม้
    max_depth=20,          # จำกัดความลึกเพื่อลด Overfitting
    min_samples_split=20,  # ขนาดขั้นต่ำสำหรับแบ่ง node
    n_jobs=-1,             # ใช้ CPU ทุก core
    random_state=42
)

# --- Model 2: Deep Neural Network ---
dnn_model = MLPRegressor(
    hidden_layer_sizes=(128, 64, 32),  # 3 ชั้น: 128 -> 64 -> 32 neurons
    activation='relu',                  # ReLU activation function
    solver='adam',                      # Adam optimizer
    alpha=0.0001,                       # L2 regularization
    batch_size=64,
    max_iter=500,
    early_stopping=True                 # หยุดเมื่อไม่ดีขึ้น
)

# --- Ensemble: Weighted Average ---
w_rf = rf_r2 / (rf_r2 + dnn_r2)    # น้ำหนักตาม R² score
w_dnn = dnn_r2 / (rf_r2 + dnn_r2)
y_pred_ensemble = (rf_pred * w_rf) + (dnn_pred * w_dnn)
```

---

### 3️⃣ `train_model_final.py` - Version สุดท้าย (Random Forest Only)

**ความแตกต่างจาก hybrid:**

- ใช้เฉพาะ Random Forest
- มี Cross-Validation (5-Fold)
- มีการตรวจสอบ Overfitting Gap

```python
# Cross-Validation
cv_scores = cross_val_score(model, X_train, y_train, cv=5, scoring='r2')
print(f"CV R² Mean: {cv_scores.mean():.4f}")

# Overfitting Check
gap = r2_train - r2_test
if gap < 0.1:
    print("✓ ดีมาก! Overfitting น้อย")
```

---

### 4️⃣ `investigate_features.py` - สืบสวน Zero Variance

**หน้าที่:** ตรวจสอบว่าทำไม LULC และ DEM ไม่ส่งผลต่อโมเดล

```python
# ตรวจสอบความแปรปรวน
for feature in features:
    print(f"{feature}: Variance = {df[feature].var():.4f}")
    if df[feature].nunique() == 1:
        print(f"⚠️ WARNING: {feature} มีค่าเดียว!")

# ผลลัพธ์ที่พบ:
# LULC: Variance = 0.0000 (Zero Variance!)
# DEM: Variance = 0.0000 (Zero Variance!)
```

**สาเหตุที่พบ:**

- การสุ่ม 30,000 จุดอาจได้พื้นที่ที่มีลักษณะคล้ายกัน
- ข้อมูลถูก broadcast ให้มี 94 เดือนแต่ค่าไม่เปลี่ยน

---

### 5️⃣ `evaluate_model.py` - ประเมินประสิทธิภาพ

```python
from sklearn.metrics import mean_squared_error, r2_score

# ทำนายค่า
y_pred = model.predict(X_test)

# คำนวณค่าความแม่นยำ
r2 = r2_score(y_test, y_pred)
rmse = np.sqrt(mean_squared_error(y_test, y_pred))

print(f"R²: {r2:.4f}")     # ผลลัพธ์: 0.7130
print(f"RMSE: {rmse:.4f}") # ผลลัพธ์: 2.04°C
```

---

## 📊 สรุป Preprocessing Pipeline (สำหรับนำเสนอ)

```
┌─────────────────────────────────────────────────────────────────────┐
│                    PREPROCESSING PIPELINE                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ① Data Collection (GEE)                                           │
│     └── ดึงข้อมูล Sentinel-5P, MODIS, ERA5                         │
│         └── 94 เดือน (2018-2025)                                   │
│                     ▼                                               │
│  ② Random Sampling                                                  │
│     └── สุ่ม 30,000 จุดภาพ → 2.8 ล้านแถว                           │
│                     ▼                                               │
│  ③ Missing Data Imputation                                         │
│     └── SimpleImputer(strategy='mean')                             │
│                     ▼                                               │
│  ④ Outlier Filtering                                               │
│     └── กรอง LST > 0 องศา → 984,274 แถว                            │
│                     ▼                                               │
│  ⑤ Feature Selection (Variance Thresholding)                       │
│     └── ตัด LULC, DEM (Zero Variance)                              │
│                     ▼                                               │
│  ⑥ Data Splitting                                                   │
│     └── 70% Train / 30% Test                                       │
│                     ▼                                               │
│  ⑦ Standardization (for DNN only)                                  │
│     └── StandardScaler: z = (x - μ) / σ                            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## ❓ คำถามที่อาจารย์อาจถาม + คำตอบ

### Q1: ทำไมใช้ Mean Imputation?

**A:**

- ง่ายและมีประสิทธิภาพสำหรับข้อมูลที่กระจายตัวใกล้เคียงปกติ
- รักษาค่ากลางของข้อมูลโดยไม่เพิ่มความเบ้
- เหมาะกับข้อมูลสภาพอากาศที่มีรูปแบบตามฤดูกาล

### Q2: ทำไมต้องตัด LULC และ DEM ออก?

**A:**

- เมื่อตรวจสอบพบว่าทั้งสองตัวแปรมี Variance = 0 (ค่าเดียวทั้งหมด)
- เกิดจากการสุ่มตัวอย่างที่ได้พื้นที่มีลักษณะคล้ายกัน
- การใช้ตัวแปรที่ไม่มีความแปรปรวนไม่ช่วยให้โมเดลเรียนรู้

### Q3: ทำไม StandardScaler ใช้กับ DNN แต่ไม่ใช้กับ RF?

**A:**

- DNN ใช้ Gradient Descent ซึ่งไวต่อขนาดของข้อมูล
- Random Forest เป็น Tree-based ซึ่งเป็น Scale-invariant (ไม่ได้รับผลกระทบ)

### Q4: 70/30 Split เหมาะสมไหม?

**A:**

- เป็นสัดส่วนมาตรฐานที่ใช้กันทั่วไป
- มีข้อมูลเพียงพอสำหรับทั้งฝึกสอน (688,991 แถว) และทดสอบ (295,283 แถว)
- ใช้ Cross-Validation (5-Fold) เพิ่มเติมเพื่อความมั่นใจ

### Q5: Early Stopping ทำงานยังไง?

**A:**

- ติดตามค่า Loss ในแต่ละ Epoch
- หากค่า Loss ไม่ลดลงติดต่อกันหลายรอบ จะหยุดการฝึกสอน
- ป้องกัน Overfitting โดยไม่ให้โมเดลจำข้อมูล Training มากเกินไป

---

## 🔧 คำสั่งรันไฟล์

```bash
# 1. รัน Training (Hybrid Model)
python train_hybrid_model.py

# 2. รัน Training (RF Final)
python train_model_final.py

# 3. ประเมินโมเดล
python evaluate_model.py

# 4. สืบสวน Features
python investigate_features.py
```

---

## 📁 ไฟล์ Output ที่สำคัญ

| ไฟล์                         | คำอธิบาย                     | ขนาด    |
| ---------------------------- | ---------------------------- | ------- |
| `df_final_processed.parquet` | ข้อมูลที่ผ่าน preprocessing  | ~38 MB  |
| `model_rf.joblib`            | โมเดล Random Forest          | ~400 MB |
| `model_dnn.joblib`           | โมเดล Deep Neural Network    | ~285 KB |
| `scaler.joblib`              | StandardScaler parameters    | ~1 KB   |
| `ensemble_weights.txt`       | ค่าน้ำหนัก RF=0.55, DNN=0.45 | ~46 B   |

---❓ คำถาม 5 ข้อที่อาจารย์อาจถาม:
ทำไมใช้ Mean Imputation?
ทำไมต้องตัด LULC และ DEM ออก?
ทำไม StandardScaler ใช้กับ DNN แต่ไม่ใช้กับ RF?
70/30 Split เหมาะสมไหม?
Early Stopping ทำงานยังไง?


